apiVersion: v1
kind: Service
metadata:
  name: {{ include "fraud-engine.fullname" . }}
  labels:
    {{- include "fraud-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "fraud-engine.selectorLabels" . | nindent 4 }}
