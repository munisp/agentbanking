apiVersion: v1
kind: Secret
metadata:
  name: {{ include "analytics-data.fullname" . }}-secrets
type: Opaque
stringData:
{{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
