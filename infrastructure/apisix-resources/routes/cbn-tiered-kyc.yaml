# cbn-tiered-kyc
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-cbn-tiered-kyc-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /cbn-tiered-kyc/*
      backends:
        - serviceName: cbn-regulatory
          servicePort: 9228
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/cbn-tiered-kyc/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
