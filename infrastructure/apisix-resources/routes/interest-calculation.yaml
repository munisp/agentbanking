# interest-calculation
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-interest-calculation-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /interest-calculation/*
      backends:
        - serviceName: core-ledger
          servicePort: 9328
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/interest-calculation/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
