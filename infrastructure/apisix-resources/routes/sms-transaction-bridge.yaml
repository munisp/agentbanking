# sms-transaction-bridge
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-sms-transaction-bridge-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /sms-transaction-bridge/*
      backends:
        - serviceName: channels-messaging
          servicePort: 9269
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/sms-transaction-bridge/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
