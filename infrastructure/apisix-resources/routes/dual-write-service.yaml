# dual-write-service
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-dual-write-service-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /dual-write/*
      backends:
        - serviceName: data-lakehouse
          servicePort: 9388
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/dual-write/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
