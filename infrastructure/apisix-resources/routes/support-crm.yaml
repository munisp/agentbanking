# support-crm
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-support-crm-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /support-crm/*
      backends:
        - serviceName: customer-engagement
          servicePort: 9380
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/support-crm/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
