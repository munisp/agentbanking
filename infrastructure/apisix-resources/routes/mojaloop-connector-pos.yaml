# mojaloop-connector-pos
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-mojaloop-connector-pos-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /mojaloop-connector-pos/*
      backends:
        - serviceName: core-payments
          servicePort: 9349
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/mojaloop-connector-pos/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
