# telemetry-ingestion
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-telemetry-ingestion-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /telemetry-ingestion/*
      backends:
        - serviceName: infra-observability
          servicePort: 9490
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/telemetry-ingestion/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
