apiVersion: v1
kind: Service
metadata:
  name: {{ include "fraud-risk.fullname" . }}
  labels:
    {{- include "fraud-risk.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "fraud-risk.selectorLabels" . | nindent 4 }}
