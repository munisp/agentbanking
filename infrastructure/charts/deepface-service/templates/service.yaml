apiVersion: v1
kind: Service
metadata:
  name: {{ include "deepface-service.fullname" . }}
  labels:
    {{- include "deepface-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "deepface-service.selectorLabels" . | nindent 4 }}
