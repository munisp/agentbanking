# store-map-service
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-store-map-service-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /store-map/*
      backends:
        - serviceName: ecommerce-marketplace
          servicePort: 9426
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/store-map/(.*)"
              - "/store-map/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
