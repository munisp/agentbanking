# billing-sla-monitor
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-billing-sla-monitor-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /billing-sla-monitor/*
      backends:
        - serviceName: billing
          servicePort: 9210
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/billing-sla-monitor/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
