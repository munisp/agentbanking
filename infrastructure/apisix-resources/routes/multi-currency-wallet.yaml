# multi-currency-wallet
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-multi-currency-wallet-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /multi-currency-wallet/*
      backends:
        - serviceName: core-accounts
          servicePort: 9317
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/multi-currency-wallet/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
