apiVersion: v1
kind: Service
metadata:
  name: {{ include "liveness-detection.fullname" . }}
  labels:
    {{- include "liveness-detection.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "liveness-detection.selectorLabels" . | nindent 4 }}
