apiVersion: v1
kind: Service
metadata:
  name: {{ include "specialized-finance.fullname" . }}
  labels:
    {{- include "specialized-finance.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "specialized-finance.selectorLabels" . | nindent 4 }}
