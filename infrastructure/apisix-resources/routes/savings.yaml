# savings — routed to account-service inside core-accounts pod
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-savings-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /savings/*
      backends:
        - serviceName: core-accounts
          servicePort: 9313
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/savings/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
