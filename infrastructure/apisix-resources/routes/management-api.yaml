# management-api
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-management-api-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /management-api/*
      backends:
        - serviceName: open-banking-api
          servicePort: 9536
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/management-api/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
