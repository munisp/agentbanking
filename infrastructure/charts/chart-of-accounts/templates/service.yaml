apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-of-accounts.fullname" . }}
  labels:
    {{- include "chart-of-accounts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "chart-of-accounts.selectorLabels" . | nindent 4 }}
