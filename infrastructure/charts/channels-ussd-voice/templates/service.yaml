apiVersion: v1
kind: Service
metadata:
  name: {{ include "channels-ussd-voice.fullname" . }}
  labels:
    {{- include "channels-ussd-voice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "channels-ussd-voice.selectorLabels" . | nindent 4 }}
