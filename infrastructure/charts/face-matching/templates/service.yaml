apiVersion: v1
kind: Service
metadata:
  name: {{ include "face-matching.fullname" . }}
  labels:
    {{- include "face-matching.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "face-matching.selectorLabels" . | nindent 4 }}
