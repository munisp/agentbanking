apiVersion: v1
kind: Service
metadata:
  name: {{ include "offline-queue.fullname" . }}
  labels:
    {{- include "offline-queue.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "offline-queue.selectorLabels" . | nindent 4 }}
