# messaging-service
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-messaging-service-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /messaging/*
      backends:
        - serviceName: channels-messaging
          servicePort: 9260
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/messaging/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
