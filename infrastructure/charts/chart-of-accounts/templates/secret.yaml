apiVersion: v1
kind: Secret
metadata:
  name: {{ include "chart-of-accounts.fullname" . }}-secrets
type: Opaque
stringData:
{{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
