apiVersion: v1
kind: Service
metadata:
  name: {{ include "transaction-ledger.fullname" . }}
  labels:
    {{- include "transaction-ledger.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "transaction-ledger.selectorLabels" . | nindent 4 }}
