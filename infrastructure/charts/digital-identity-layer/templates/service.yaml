apiVersion: v1
kind: Service
metadata:
  name: {{ include "digital-identity-layer.fullname" . }}
  labels:
    {{- include "digital-identity-layer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "digital-identity-layer.selectorLabels" . | nindent 4 }}
