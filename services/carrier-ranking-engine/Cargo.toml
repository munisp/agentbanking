[package]
name = "carrier-ranking-engine"
version = "1.0.0"
edition = "2021"
description = "Multi-factor carrier ranking engine with weighted scoring"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
