# carrier-live-api
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-carrier-live-api-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /carrier-live-api/*
      backends:
        - serviceName: connectivity-sim
          servicePort: 9295
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/carrier-live-api/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
