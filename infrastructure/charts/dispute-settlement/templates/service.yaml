apiVersion: v1
kind: Service
metadata:
  name: {{ include "dispute-settlement.fullname" . }}
  labels:
    {{- include "dispute-settlement.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "dispute-settlement.selectorLabels" . | nindent 4 }}
