# ecommerce-catalog-go
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-ecommerce-catalog-go-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /ecommerce-catalog/*
      backends:
        - serviceName: ecommerce-marketplace
          servicePort: 9417
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/ecommerce-catalog/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
