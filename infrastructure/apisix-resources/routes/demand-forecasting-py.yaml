# demand-forecasting-py
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-demand-forecasting-py-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /demand-forecasting/*
      backends:
        - serviceName: ai-ml
          servicePort: 9152
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/demand-forecasting/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
