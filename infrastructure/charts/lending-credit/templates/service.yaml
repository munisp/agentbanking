apiVersion: v1
kind: Service
metadata:
  name: {{ include "lending-credit.fullname" . }}
  labels:
    {{- include "lending-credit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "lending-credit.selectorLabels" . | nindent 4 }}
