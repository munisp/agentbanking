# compensating-actions
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-compensating-actions-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /compensating-actions/*
      backends:
        - serviceName: workflow-orchestration
          servicePort: 9601
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/compensating-actions/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
