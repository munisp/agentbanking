# zapier-integration
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-zapier-integration-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /zapier-integration/*
      backends:
        - serviceName: third-party-integrations
          servicePort: 9597
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/zapier-integration/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
