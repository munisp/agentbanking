# core-banking
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-core-banking-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /core-banking/*
      backends:
        - serviceName: core-ledger
          servicePort: 9326
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/core-banking/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
