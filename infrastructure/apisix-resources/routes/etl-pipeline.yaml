# etl-pipeline
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-etl-pipeline-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /etl-pipeline/*
      backends:
        - serviceName: data-lakehouse
          servicePort: 9389
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/etl-pipeline/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
