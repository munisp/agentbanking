apiVersion: v1
kind: Service
metadata:
  name: {{ include "realtime-transactions.fullname" . }}
  labels:
    {{- include "realtime-transactions.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "realtime-transactions.selectorLabels" . | nindent 4 }}
