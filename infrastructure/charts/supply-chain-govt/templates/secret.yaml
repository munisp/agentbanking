apiVersion: v1
kind: Secret
metadata:
  name: {{ include "supply-chain-govt.fullname" . }}-secrets
type: Opaque
stringData:
{{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
