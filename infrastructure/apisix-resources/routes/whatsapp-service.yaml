# whatsapp-service
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-whatsapp-service-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /whatsapp/*
      backends:
        - serviceName: channels-social
          servicePort: 9253
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/whatsapp/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
