apiVersion: v1
kind: Service
metadata:
  name: {{ include "workflow-orchestration.fullname" . }}
  labels:
    {{- include "workflow-orchestration.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "workflow-orchestration.selectorLabels" . | nindent 4 }}
