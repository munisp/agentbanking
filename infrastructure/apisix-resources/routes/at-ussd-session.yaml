# at-ussd-session
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-at-ussd-session-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /at-ussd-session/*
      backends:
        - serviceName: channels-ussd-voice
          servicePort: 9276
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/at-ussd-session/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
