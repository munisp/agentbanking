# resilience-proxy
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-resilience-proxy-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /resilience-proxy/*
      backends:
        - serviceName: infra-network
          servicePort: 9477
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/resilience-proxy/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
