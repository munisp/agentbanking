apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-pos.fullname" . }}
  labels:
    {{- include "agent-pos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "agent-pos.selectorLabels" . | nindent 4 }}
