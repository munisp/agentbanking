# government-integration
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-government-integration-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /government-integration/*
      backends:
        - serviceName: supply-chain-govt
          servicePort: 9586
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/government-integration/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
