# pos-fluvio-consumer
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-pos-fluvio-consumer-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /pos-fluvio-consumer/*
      backends:
        - serviceName: agent-pos
          servicePort: 9134
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/pos-fluvio-consumer/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
