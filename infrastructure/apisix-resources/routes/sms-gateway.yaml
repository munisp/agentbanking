# sms-gateway
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-sms-gateway-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /sms-gateway/*
      backends:
        - serviceName: channels-messaging
          servicePort: 9267
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/sms-gateway/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
