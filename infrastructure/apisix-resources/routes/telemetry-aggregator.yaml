# telemetry-aggregator
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-telemetry-aggregator-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /telemetry-aggregator/*
      backends:
        - serviceName: infra-observability
          servicePort: 9487
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/telemetry-aggregator/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
