# voice-command-nlu
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-voice-command-nlu-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /voice-command-nlu/*
      backends:
        - serviceName: channels-ussd-voice
          servicePort: 9288
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/voice-command-nlu/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
