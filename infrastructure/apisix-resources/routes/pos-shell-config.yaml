# pos-shell-config
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-pos-shell-config-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /pos-shell-config/*
      backends:
        - serviceName: agent-pos
          servicePort: 9140
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/pos-shell-config/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
