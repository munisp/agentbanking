# edge-computing
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-edge-computing-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /edge-computing/*
      backends:
        - serviceName: infra-network
          servicePort: 9465
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/edge-computing/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
