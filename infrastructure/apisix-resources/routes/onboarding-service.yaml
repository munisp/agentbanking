# onboarding-service
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-onboarding-service-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /onboarding/*
      backends:
        - serviceName: core-accounts
          servicePort: 9318
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/onboarding/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
