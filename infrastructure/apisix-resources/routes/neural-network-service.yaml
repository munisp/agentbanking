# neural-network-service
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-neural-network-service-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /neural-network/*
      backends:
        - serviceName: ai-ml
          servicePort: 9157
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/neural-network/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
