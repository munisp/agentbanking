apiVersion: v1
kind: Service
metadata:
  name: {{ include "analytics-data.fullname" . }}
  labels:
    {{- include "analytics-data.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "analytics-data.selectorLabels" . | nindent 4 }}
