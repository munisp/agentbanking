apiVersion: v1
kind: Secret
metadata:
  name: {{ include "user-management.fullname" . }}-secrets
type: Opaque
stringData:
{{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
