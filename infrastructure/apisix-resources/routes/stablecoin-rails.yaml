# stablecoin-rails
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-stablecoin-rails-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /stablecoin-rails/*
      backends:
        - serviceName: specialized-finance
          servicePort: 9579
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/stablecoin-rails/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
