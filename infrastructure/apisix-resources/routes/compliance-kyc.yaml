# compliance-kyc
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-compliance-kyc-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /compliance-kyc/*
      backends:
        - serviceName: cbn-regulatory
          servicePort: 9230
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/compliance-kyc/(.*)"
              - "/api/v1/compliance-kyc/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
