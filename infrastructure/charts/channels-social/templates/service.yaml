apiVersion: v1
kind: Service
metadata:
  name: {{ include "channels-social.fullname" . }}
  labels:
    {{- include "channels-social.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "channels-social.selectorLabels" . | nindent 4 }}
