[package]
name = "ussd-session-cache"
version = "1.0.0"
edition = "2021"
description = "High-performance USSD session state cache with TTL expiry"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
