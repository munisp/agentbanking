[package]
name = "bandwidth-optimizer"
version = "1.0.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = "z"
lto = true
strip = true
actix-web = { version = "4", optional = true }
