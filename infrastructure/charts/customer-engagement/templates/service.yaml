apiVersion: v1
kind: Service
metadata:
  name: {{ include "customer-engagement.fullname" . }}
  labels:
    {{- include "customer-engagement.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "customer-engagement.selectorLabels" . | nindent 4 }}
