# data-warehouse
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-data-warehouse-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /data-warehouse/*
      backends:
        - serviceName: data-lakehouse
          servicePort: 9387
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/data-warehouse/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
