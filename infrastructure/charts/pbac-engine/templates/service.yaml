apiVersion: v1
kind: Service
metadata:
  name: {{ include "pbac-engine.fullname" . }}
  labels:
    {{- include "pbac-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "pbac-engine.selectorLabels" . | nindent 4 }}
