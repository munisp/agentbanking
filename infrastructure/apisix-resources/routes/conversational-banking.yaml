# conversational-banking
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-conversational-banking-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /conversational-banking/*
      backends:
        - serviceName: channels-ussd-voice
          servicePort: 9277
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/conversational-banking/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
