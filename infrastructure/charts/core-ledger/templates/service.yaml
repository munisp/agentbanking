apiVersion: v1
kind: Service
metadata:
  name: {{ include "core-ledger.fullname" . }}
  labels:
    {{- include "core-ledger.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "core-ledger.selectorLabels" . | nindent 4 }}
