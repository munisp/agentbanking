apiVersion: v1
kind: Service
metadata:
  name: {{ include "sim-orchestrator-service.fullname" . }}
  labels:
    {{- include "sim-orchestrator-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "sim-orchestrator-service.selectorLabels" . | nindent 4 }}
