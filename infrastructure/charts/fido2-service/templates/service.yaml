apiVersion: v1
kind: Service
metadata:
  name: {{ include "fido2-service.fullname" . }}
  labels:
    {{- include "fido2-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "fido2-service.selectorLabels" . | nindent 4 }}
