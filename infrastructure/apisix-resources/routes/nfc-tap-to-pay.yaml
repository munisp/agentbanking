# nfc-tap-to-pay
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: 54agent-nfc-tap-to-pay-route
  namespace: 54agent
spec:
  ingressClassName: apisix
  http:
    - name: rule-1
      priority: 10
      match:
        hosts:
          - 54agent.upi.dev
        paths:
          - /nfc-tap-to-pay/*
      backends:
        - serviceName: core-payments
          servicePort: 9351
      plugins:
        - name: proxy-rewrite
          enable: true
          config:
            regex_uri:
              - "^/nfc-tap-to-pay/(.*)"
              - "/$1"
        - name: cors
          enable: true
          config:
            allow_origins: "*"
            allow_methods: "*"
            allow_headers: "*"
            expose_headers: "*"
