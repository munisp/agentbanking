apiVersion: v1
kind: Service
metadata:
  name: {{ include "third-party-integrations.fullname" . }}
  labels:
    {{- include "third-party-integrations.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    {{- range .Values.service.ports }}
    - port: {{ .port }}
      targetPort: {{ .port }}
      protocol: TCP
      name: port-{{ .port }}
    {{- end }}
  selector:
    {{- include "third-party-integrations.selectorLabels" . | nindent 4 }}
